import 'package:flutter/material.dart';
class SaveAddress extends StatelessWidget {
  const SaveAddress();

  @override
  Widget build(BuildContext context) {
    return  Scaffold(appBar: AppBar(title: Text("Saved Address"),centerTitle: true,backgroundColor: Colors.white,),);
  }
}
