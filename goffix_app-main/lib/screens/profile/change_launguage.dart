import 'package:flutter/material.dart';
class ChangeLaunguage extends StatelessWidget {
  const ChangeLaunguage();

  @override
  Widget build(BuildContext context) {
    return  Scaffold(appBar: AppBar(title: Text("Change Launguage"),centerTitle: true,backgroundColor: Colors.white,),);
  }
}
